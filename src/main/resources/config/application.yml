server:
  servlet:
    context-path: /admin
  port: 8081
spring:
  config:
    name:
  application:
    name: fast-admin
  profiles:
    active: @profileActive@
fast-code:
  swagger:
    api-scans:
      - group-name: admin
        base-package: pers.dandandog.admin.controller
        base-path: /sys
  map-struct:
    url-domain: https://www.myelephant.cn